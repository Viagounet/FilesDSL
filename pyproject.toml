[project]
name = "filesdsl"
version = "0.1.0"
description = "A safe file-exploration DSL for LLM agents"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymupdf>=1.26.4",
]

[project.scripts]
filesdsl = "filesdsl.__main__:main"
